# Everything generated by a bench run: the extracted hover payloads and the
# summary beside them. `rm -rf results/` restores a clean tree. The throwaway
# multi-dictionary database never lands here - `hover_parse_bench build`
# writes it to a scratch directory you name.
results/
